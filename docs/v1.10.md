## dev: Update v1.10

### 1. feat: adjust header components on mobile screens

- Refined the header layout and component behavior for mobile screen sizes
- Improved responsive spacing, alignment, and usability on smaller screens
- Preserved the existing desktop header appearance and behavior
- Files: `next.config.ts`, `src/components/download/Header.tsx`, `src/components/download/ProfileToggle.tsx`

### 2. feat(chore): switch from npm to pnpm

- Migrated the project package manager from npm to pnpm
- Added `pnpm-lock.yaml` to track dependency versions and installation state
- Added `pnpm-workspace.yaml` for pnpm workspace configuration
- Removed `package-lock.json` to eliminate conflicting package-manager lockfiles
- Provides a consistent and efficient dependency installation workflow
- Files: `package-lock.json` (removed), `pnpm-lock.yaml`, `pnpm-workspace.yaml`

---

**Summary:** This PR covers the v1.10 update — improves the header experience on mobile screens while preserving the desktop layout and migrates the project from npm to pnpm for a consistent dependency management workflow. The release includes responsive header refinements and the package-manager configuration required for pnpm-based development.
