## dev: Update v1.08

### 1. feat: Text changes — version upstream + noted issue in TMGraph.tsx

- Version upstreamed from v1.07 to v1.08
- Directory rename
- Flagged a known issue for future work: when the current year changes, TM Graph's "Today" column label updates, but the highlighted (transition-year) squares for that year do not update along with it

### 2. feat: New CSS variables and Tailwind classes for repetitive use in globals.css

- Introduced reusable design tokens to replace scattered repeated values

### 3. feat(feature): Filter chips introduced to the download tree, alongside the search bar

- Filtering improved by combining the existing search bar algorithm with new filter chip facets
- `FilterChipBar` renders the filter chips above the search bar
- `tree-filters.ts` contains the underlying filtering logic
- Open for further optimization and modification
- Files: `DownloadPage.tsx`, `DownloadTree.tsx`, `FilterChipBar.tsx` (new), `tree-filters.ts`

### 4. feat(ui): Colors and animations synced with Tailwind classes (globals.css)

- Committed files converted to use Tailwind color classes and animations instead of hardcoded values
- Supports responsiveness for future theming
- Files: `globals.css`, `DownloadTree.tsx`, `FilterChipBar.tsx`, `Header.tsx`, `ProfileToggle.tsx`, `SearchBar.tsx`, `PrintYearOverview.tsx`, `TMGraph.tsx`
- All hardcoded values replaced with Tailwind classes

### 5. feat(ui): UI modifications

- Text modifications
- Color changes synced with Tailwind (globals.css)
- Animations, transitions, and smooth scroller movements added

### 6. feat(fix): Visual fixes and structural adjustments

- Facet order shuffled
- New filter indicator added
- Dropdown UI fixes
- Modern scrollbar added
- Font weight changes on selection/deselection

### 7. feat(fix): Major structural changes in tree-filters

- Book Type label overrides
- Custom Book Type ordering
- Book Type name trimming and custom name overrides
- Additional structural changes
- Open to further modifications

### 8. feat: Textbooks added as a Book Type filter selector

- Added Textbooks as a selectable Book Type filter value
- Selecting Textbooks filters out unrelated roots and retains its category branches
- Textbooks can automatically expand the relevant book tree

### 9. feat(ui): Filter logic passed through tree components

- Passed active filter state through component props
- Added filter-aware expansion for manual tree navigation
- Preserved manual branch opening while expanding only the required filtered path

### 10. feat(fix): Consistent folder expansion rules for filters

- Textbooks opens to show Grade 1–5, Grade 6–11, and General English
- Pirivena Books opens to show its Grade 1–5 children
- Activity Books, Essential Learning Books, Further Learning Books, Transversal Skills, and Common English open through the selected Book Type
- Combined Book Type, Medium, Grade, and Term filters expand through the required path
- Unrelated surviving branches remain collapsed
- Text search continues to fully expand matching paths
- Manual folder controls remain available when no filter forces expansion
- Clearing or replacing filters does not leave stale forced expansion

---

**Summary:** This PR covers the v1.08 update — introduces the new filter chip system (search bar + facet-based filtering working together), a centralized design token system (colors, animations) in `globals.css` applied across all major components, UI polish (text, motion, scrollbars, selection states), and structural refinements to the Book Type facet (label overrides, ordering, trimming). One known issue is documented inline in `TMGraph.tsx` for follow-up. Several areas (tree-filters, filter chips) are explicitly left open for further iteration.
